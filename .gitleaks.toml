[extend]
useDefault = true

[[allowlists]]
description = "Empty provider-key placeholders in the committed environment template"
condition = "AND"
regexTarget = "secret"
paths = ['''(?:^|/)\.env\.example$''']
regexes = ['''^ELEVENLABS_API_KEY=$''']
